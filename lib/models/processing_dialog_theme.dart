import 'package:flutter/material.dart';

class ProcessingDialogThemeModel {
  final Widget icon;
  final String message;
  final Color color;
  final Color waveColor;

  ProcessingDialogThemeModel(
      {this.icon, this.color, this.waveColor, this.message});
}
