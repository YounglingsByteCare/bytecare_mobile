import 'package:flutter/material.dart';

class RegistrationStepAddress extends StatefulWidget {
  @override
  _RegistrationStepAddressState createState() =>
      _RegistrationStepAddressState();
}

class _RegistrationStepAddressState extends State<RegistrationStepAddress> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
